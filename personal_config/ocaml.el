(add-hook 'tuareg-mode-hook
	  (lambda () (progn
		       (local-set-key (kbd "C-c C-r") 'utop-eval-region)
		       (local-set-key (kbd "C-c C-j") 'utop-eval-buffer)
		       (local-set-key (kbd "C-c C-"
		       )

)
	  )
